using UnityEngine;

public class StepPrediction : MonoBehaviour
{
    
    //private void StepPrediction(bool walkForward, bool walkBackward)
    //{
    //    //Reset variables when balanced
    //    if (!walkForward && !walkBackward)
    //    {
    //        ResetWalkCycle();
    //    }

    //    //Check direction to walk when off balance
    //    //Walk backward
    //    if (COMP.position.z < playerParts[11].transform.position.z && COMP.position.z < playerParts[12].transform.position.z)
    //    {
    //        walkBackward = true;
    //    }
    //    else
    //    {
    //        if (!isKeyDown)
    //        {
    //            walkBackward = false;
    //        }
    //    }

    //    //Walk forward
    //    if (COMP.position.z > playerParts[11].transform.position.z && COMP.position.z > playerParts[12].transform.position.z)
    //    {
    //        walkForward = true;
    //    }
    //    else
    //    {
    //        if (!isKeyDown)
    //        {
    //            walkForward = false;
    //        }
    //    }
    //}
}